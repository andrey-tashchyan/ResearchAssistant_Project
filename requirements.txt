# Core data and file handling
pandas>=2.1.0
numpy>=1.26.0
openpyxl>=3.1.2
pyarrow>=15.0.0

# Logging and progress tracking
tqdm>=4.66.0
loguru>=0.7.2

# File system and path utilities
pathlib>=1.0.1

# Optional text processing and fuzzy matching (used in code mapping)
regex>=2023.10.3
rapidfuzz>=3.3.1

# Optional plotting / summaries (for panel_summary.csv stats)
matplotlib>=3.8.0

# System interaction (used in psid_tool / run_all.sh integration)
argparse>=1.4.0

# Optional scientific extensions (safe for EPFL macOS M2 environments)
scipy>=1.12.0

# Development / testing
pytest>=8.0.0
ipython>=8.20.0